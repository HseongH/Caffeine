// LIBRARY
import React from 'react';

// COMPONENTS
import Layout from './Layout';

const Post = (props) => {
  return <Layout width={'100%'} height={'100px'} radius={'10px'} bg={'#fff'}></Layout>;
};

Post.defaultProps = {};

export default Post;
